let x : Int = 5;
fn double(a : Int) -> Int 
{
    return a * 2;
}

fn applyTwice(
    func : fn(Int) -> Int,
    input : Int
) -> Int 
{
    return func(func(input));
}

print(applyTwice(double, 5));

print(fib(1, 1, 10));

